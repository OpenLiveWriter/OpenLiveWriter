<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <AssemblyName>PostBuild.CreateInstaller</AssemblyName>
    <RootNamespace>PostBuild.CreateInstaller</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <Compile Include="..\GlobalAssemblyInfo.cs" Link="GlobalAssemblyInfo.cs" />
    <Compile Include="**\*.cs" Exclude="obj\**" />
  </ItemGroup>

  <ItemGroup>
    <None Include="..\..\..\OpenLiveWriter.nuspec" Link="OpenLiveWriter.nuspec" />
    <Content Include="createinstaller.cmd" />
    <Content Include="ReadMe.txt" />
  </ItemGroup>
</Project>